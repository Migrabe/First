---
name: verify-prompt-logic
description: Проводит воспроизводимый аудит логики генерации промптов: проверяет поток данных UI→state→prompt, конфликты опций, синтаксис по моделям и формирует findings-first отчет с pass/fail критериями.
---

# Prompt Generation Logic Audit Skill

## Goal
Провести глубокий анализ кодовой базы, чтобы убедиться, что генератор промптов создает корректные, логичные и совместимые запросы для нейросетей (Text-to-Image / Text-to-Video).

## Scope (Что обязательно проверить)
- Все активные модели/движки, реально используемые в проекте.
- Все форматы вывода промпта, доступные в UI.
- Ключевые сценарии: максимум опций, конфликтные опции, минимальный ввод.

## Pass/Fail Criteria
- **PASS**: нет синтаксических ошибок для каждой проверенной модели, нет потерь данных в цепочке UI→state→output, конфликты опций обрабатываются предсказуемо.
- **FAIL**: найден хотя бы один воспроизводимый кейс, где:
  - ломается генерация (синтаксис/параметры модели),
  - теряются выбранные пользователем значения,
  - конфликтные опции дают противоречивый или невалидный итоговый промпт.

## Audit Checklist (Что проверять детально)

### 1. Logic & Flow Consistency (Сохранение логики)
- [ ] **Цепочка выбора:** Проверь, как передаются данные от выбора пользователя (UI/Input) до финальной строки промпта. Нет ли потери данных?
- [ ] **Ветвление (Options Logic):** Проверь логику `if/else`. Если пользователь выбирает "Кинематографичный стиль", не блокирует ли это выбор "Соотношения сторон"?
- [ ] **Комбинаторика:** Убедись, что взаимоисключающие опции (например, "Черно-белое фото" и "Насыщенные цвета") не могут быть выбраны одновременно или обрабатываются корректно.

### 2. Syntax & Compatibility (Совместимость)
- [ ] **Формат моделей:** Проверь синтаксис для конкретных моделей:
    - *Midjourney:* Проверь параметры `--ar`, `--v`, `--s`, `--no`. Нет ли лишних пробелов?
    - *Nano Banana Pro:* Проверь целостность и правильность финального промпта пройдя по всем опциям выбора, посмотри приоритетный порядок в финальном промпте.
    - *ChatGpt image:* Проверь целостность и правильность финального промпта пройдя по всем опциям выбора, посмотри приоритетный порядок в финальном промпте.
    - *Stable Diffusion:* Проверь формат весов `(keyword:1.2)` или `[keyword]`.
    - *Runway/Pika/Kling/Veo:* Проверь параметры движения (motion buckets, camera zoom) и длительности.
- [ ] **Конфликты токенов:** Проверь код на дублирование ключевых слов в финальной строке.
- [ ] **Покрытие движков:** Для каждого движка (включая Video: Kling, Veo, Seedance), явно укажи: `checked / not checked` и причину.

### 3. Error Handling (Обработка ошибок)
- [ ] Как программа ведет себя, если пользователь не выбрал ни одной опции? (Должен быть дефолтный пресет).
- [ ] Есть ли защита от инъекций (если ввод свободный), чтобы пользователь не сломал JSON-структуру или запрос к API.

## Instructions
1. **Analyze**: Прочитай файлы, отвечающие за сборку промпта и конфликт-логику.
2. **Simulate (обязательно письменно, не только мысленно):** Проведи 3 сценария:
    - *Сценарий А:* Пользователь выбрал все возможные опции.
    - *Сценарий Б:* Пользователь выбрал конфликтующие стили.
    - *Сценарий В:* Минимальный выбор (только тема).
3. **Validate**: Для каждого сценария зафиксируй `expected` и `actual`.
4. **Report**: Создай отчет `AUDIT_REPORT.md` в формате findings-first:
    - 🔴 **Critical**: Ломает генерацию (ошибка синтаксиса).
    - 🟡 **Warning**: Логический конфликт (например, "реализм" + "векторная графика").
    - 🟢 **Optimization**: Советы по улучшению качества промпта.
5. Для каждого finding обязательно укажи:
   - путь к файлу,
   - строку/блок,
   - шаги воспроизведения,
   - impact,
   - рекомендацию.

## Report Template (обязательный минимум)
1. **Scope & Inputs**
2. **Findings (Critical → Warning → Optimization)**
3. **Reproduction Steps**
4. **Open Questions / Assumptions**
5. **Final Verdict: PASS/FAIL**

## Constraints
- Не исправляй код автоматически до отчета, если нет явного запроса на фиксы.
- Игнорируй файлы конфигурации `venv` и `.git`.
